# Voice Thief — End-to-End Test Report

Generated by `node vt-e2e.cjs` against `npm run dev` with `VT_MOCK_AI=1`.

**Result: 38 / 38 checks pass.**

The script drives the running dev server via Playwright (headless Chromium
with swiftshader) and captures screenshots at each milestone into
`docs/test-shots/`.

## Coverage

### Phase 1 — Title → Intro → Playing
- ✓ initial phase = title
- ✓ `__vt` debug hook exposed on window
- ✓ after Start button: phase = intro
- ✓ after Skip button: phase = playing
- ✓ initial in-game time near 6:00 PM (within 1 game-min of start)
- ✓ player at street
- ✓ inventory empty
- ✓ suspicion = 0
- ✓ vault closed
- ✓ bank front unlocked at 6 PM
- ✓ hallway locked at start

### Phase 2 — Recording flow
- ✓ `/api/clone` returned 200
- ✓ voice id is mock
- ✓ inventory has 1 card
- ✓ card is calm-tagged manager

### Phase 3 — Phone diversion
- ✓ manager branch flipped to `rushedHome` via wife→manager call
- ✓ hallway unlocked as side effect
- ✓ phone modal auto-closed after NPC hangup

### Phase 4 — Voice authentication
- ✓ stressed voice fails vault
- ✓ suspicion increased after fail (+15)
- ✓ calm voice opens vault

### Phase 5 — Briefcase + Win
- ✓ briefcase taken
- ✓ player has briefcase
- ✓ phase = won after reaching station with briefcase

### Phase 6 — Failure path: alarm
- ✓ reset back to playing/start state
- ✓ alarm triggered after suspicion ≥ 100
- ✓ phase = lost on alarm

### Phase 7 — Failure path: time-out
- ✓ phase = lost at 9 PM

### Phase 8 — Solution C primitive
- ✓ Solution C: manager branch flipped to `atCafe` via secretary→manager
  "ledger at cafe" call

### Phase 9 — Direct API contracts
- ✓ `/api/bootstrap` returns mock agents (4 of them)
- ✓ `/api/tts` returns audio/mpeg (992 bytes for "hello")
- ✓ `/api/auth-voice` rejects wrong NPC voice
- ✓ `/api/auth-voice` accepts calm bankManager voice
- ✓ `/api/auth-voice` rejects panicked bankManager voice
- ✓ `/api/conversation` wife→manager break-in triggers hangup with "Maggie" reply
- ✓ `/api/conversation` manager voice using "Harold" raises suspicion (+15)
- ✓ `/api/conversation` vault code probe → +30 susp + hangup
- ✓ `/api/cleanup` no-ops on mock voice ids

## Reproducing locally

```bash
VT_MOCK_AI=1 npm run dev    # in one terminal
node vt-e2e.cjs              # in another
```

Captured screenshots end up in `docs/test-shots/` for inspection. The README
tour uses the curated `docs/screenshots/` set generated by `vt-fp-shots.cjs`.
